# Local dev: leave blank to use the Vite dev proxy (see vite.config.js) to http://localhost:8000
# Production (Vercel): set this to your deployed backend URL, e.g.
#   VITE_API_BASE_URL=https://aivoa-backend.onrender.com
VITE_API_BASE_URL=
